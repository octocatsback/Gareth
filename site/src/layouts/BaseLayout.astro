---
import '../styles/global.css';

interface Props {
  title?: string;
  description?: string;
}

const {
  title = 'Mona\'s GitHub Info',
  description = 'A bright Astro website that highlights GitHub docs, blog stories, and changelog updates.',
} = Astro.props;
const faviconUrl = `${import.meta.env.BASE_URL}favicon.svg`;
const gridUrl = `${import.meta.env.BASE_URL}graphics/grid.svg`;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link rel="icon" type="image/svg+xml" href={faviconUrl} />
    <title>{title}</title>
    <meta name="description" content={description} />
  </head>
  <body style={`--grid-image: url("${gridUrl}")`}>
    <slot />
  </body>
</html>
